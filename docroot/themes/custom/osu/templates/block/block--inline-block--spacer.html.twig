{#
/**
 * @file
 * Theme override to display a block.
 *
 * Available variables:
 * - plugin_id: The ID of the block implementation.
 * - label: The configured label of the block if visible.
 * - configuration: A list of the block's configuration values.
 *   - label: The configured label for the block.
 *   - label_display: The display settings for the label.
 *   - provider: The module or other provider that provided this block plugin.
 *   - Block plugin specific settings will also be stored here.
 * - content: The content of this block.
 * - attributes: array of HTML attributes populated by modules, intended to
 *   be added to the main container tag of this template.
 *   - id: A valid HTML ID and guaranteed unique.
 * - title_attributes: Same as attributes, except applied to the main title
 *   tag that appears in the template.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 *
 * @see template_preprocess_block()
 */
#}
<div{{ attributes.addClass('block-spacer') }}>
  {{ title_prefix }}
  {{ title_suffix }}
  {% if content['#block_content'].field_spacer_mobile_padding[0].value is not empty %}
    {% set mobile_padding = content['#block_content'].field_spacer_mobile_padding[0].value %}
    <div class="block-spacer__mobile-spacer mobile-only" style="padding-top: {{ mobile_padding }}px"></div>
  {% endif %}

  {% if content['#block_content'].field_spacer_tablet_padding[0].value is not empty %}
    {% set tablet_padding = content['#block_content'].field_spacer_tablet_padding[0].value %}
    <div class="block-spacer__tablet-spacer tablet-only" style="padding-top: {{ tablet_padding }}px"></div>
  {% endif %}

  {% if content['#block_content'].field_spacer_desktop_padding[0].value is not empty %}
    {% set desktop_padding = content['#block_content'].field_spacer_desktop_padding[0].value %}
    <div class="block-spacer__desktop-spacer desktop-only" style="padding-top: {{ desktop_padding }}px"></div>
  {% endif %}

  {% if content['#block_content'].field_spacer_lg_desktop_padding[0].value is not empty %}
    {% set lg_desktop_padding = content['#block_content'].field_spacer_lg_desktop_padding[0].value %}
    <div class="block-spacer__large-desktop-spacer lg-desktop-only" style="padding-top: {{ lg_desktop_padding }}px"></div>
  {% endif %}

  <div class="text-marker flex-container flex-dir-column flex-jc-center flex-ai-center">Spacer component</div>
</div>
